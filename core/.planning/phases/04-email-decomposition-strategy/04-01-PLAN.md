---
phase: 04-email-decomposition-strategy
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/riven/core/service/identity/EmailMatcher.kt
  - src/test/kotlin/riven/core/service/identity/EmailMatcherTest.kt
autonomous: true
requirements: [UTIL-03, UTIL-04, CAND-06, TEST-04]

must_haves:
  truths:
    - "EmailMatcher.extractDomain('john@acme.com') returns 'acme.com'"
    - "EmailMatcher.extractLocal('john@acme.com') returns 'john'"
    - "EmailMatcher.tokenizeLocal('john.smith') returns ['john', 'smith']"
    - "EmailMatcher.localPartSimilarity('john.smith', 'smith.john') returns 1.0"
    - "EmailMatcher.isFreeEmailDomain('gmail.com') returns true"
    - "EmailMatcher.isFreeEmailDomain('acme.com') returns false"
  artifacts:
    - path: "src/main/kotlin/riven/core/service/identity/EmailMatcher.kt"
      provides: "Email parsing, tokenization, similarity, free-domain check"
      contains: "object EmailMatcher"
    - path: "src/test/kotlin/riven/core/service/identity/EmailMatcherTest.kt"
      provides: "Unit tests covering all EmailMatcher public methods"
      min_lines: 60
  key_links:
    - from: "src/main/kotlin/riven/core/service/identity/EmailMatcher.kt"
      to: "src/main/kotlin/riven/core/service/identity/TokenSimilarity.kt"
      via: "TokenSimilarity.overlap() called from localPartSimilarity()"
      pattern: "TokenSimilarity\\.overlap"
---

<objective>
Create the EmailMatcher Kotlin object utility with email parsing, local-part tokenization, overlap-based similarity scoring, and free email domain detection.

Purpose: Provides the core email decomposition logic that Plan 02 will wire into the candidate discovery pipeline. Follows the same Kotlin object pattern as NicknameExpander and TokenSimilarity from Phase 3.
Output: EmailMatcher.kt + EmailMatcherTest.kt
</objective>

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-email-decomposition-strategy/04-CONTEXT.md
@.planning/phases/04-email-decomposition-strategy/04-RESEARCH.md

<interfaces>
<!-- Reused from Phase 3 — TokenSimilarity.overlap() is the core dependency -->

From src/main/kotlin/riven/core/service/identity/TokenSimilarity.kt:
```kotlin
object TokenSimilarity {
    /**
     * Computes the overlap coefficient between [a] and [b] treated as word token sets.
     * Tokenization splits on whitespace, lowercases, and filters empty tokens.
     * Returns 0.0 if either input produces an empty token set.
     */
    fun overlap(a: String, b: String): Double
}
```

From src/main/kotlin/riven/core/service/identity/NicknameExpander.kt (structural pattern reference):
```kotlin
object NicknameExpander {
    private val GROUPS: List<Set<String>> = listOf(...)
    private val INDEX: Map<String, Set<String>> = buildIndex()
    fun expand(name: String): Set<String>
    fun areEquivalent(a: String, b: String): Boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EmailMatcher object + unit tests (TDD)</name>
  <files>src/main/kotlin/riven/core/service/identity/EmailMatcher.kt, src/test/kotlin/riven/core/service/identity/EmailMatcherTest.kt</files>
  <behavior>
    - extractDomain("john@acme.com") returns "acme.com"
    - extractDomain("john") returns null (no @ sign)
    - extractDomain("") returns null
    - extractLocal("john@acme.com") returns "john"
    - extractLocal("john.smith@acme.com") returns "john.smith"
    - extractLocal("noatsign") returns null
    - tokenizeLocal("john.smith") returns ["john", "smith"]
    - tokenizeLocal("john_smith") returns ["john", "smith"]
    - tokenizeLocal("john-smith") returns ["john", "smith"]
    - tokenizeLocal("jsmith") returns ["jsmith"] (no heuristic splitting)
    - tokenizeLocal("john..smith") returns ["john", "smith"] (empty tokens filtered)
    - tokenizeLocal("JOHN.SMITH") returns ["john", "smith"] (lowercased)
    - localPartSimilarity("john.smith", "smith.john") returns 1.0 (full overlap)
    - localPartSimilarity("john.smith", "j.smith") returns 0.5 (partial overlap)
    - localPartSimilarity("john.smith", "bob.jones") returns 0.0 (no overlap)
    - isFreeEmailDomain("gmail.com") returns true
    - isFreeEmailDomain("yahoo.com") returns true
    - isFreeEmailDomain("acme.com") returns false
    - isFreeEmailDomain("GMAIL.COM") returns true (case-insensitive)
  </behavior>
  <action>
    RED: Create EmailMatcherTest.kt with tests for all behaviors above. Tests grouped by method: extractDomain, extractLocal, tokenizeLocal, localPartSimilarity, isFreeEmailDomain. Run tests — all must fail (EmailMatcher.kt does not exist yet).

    GREEN: Create EmailMatcher.kt as a Kotlin `object` in `riven.core.service.identity` package:

    1. `FREE_EMAIL_DOMAINS` as a private `Set<String>` constant with 31 entries (see RESEARCH.md for complete list): gmail.com, yahoo.com, outlook.com, hotmail.com, aol.com, icloud.com, protonmail.com, proton.me, live.com, msn.com, yahoo.co.uk, yahoo.fr, yahoo.de, yahoo.es, yahoo.it, ymail.com, mail.com, gmx.com, gmx.de, gmx.net, web.de, mail.ru, qq.com, yandex.com, yandex.ru, comcast.net, att.net, verizon.net, sbcglobal.net, me.com, mac.com, zoho.com, fastmail.com, tutanota.com.

    2. `extractDomain(email: String): String?` — find last `@` index, return substring after it lowercased, or null if no `@`.

    3. `extractLocal(email: String): String?` — find last `@` index, return substring before it, or null if no `@`.

    4. `tokenizeLocal(local: String): List<String>` — `local.lowercase().split(Regex("[._-]")).filter { it.isNotEmpty() }`.

    5. `localPartSimilarity(a: String, b: String): Double` — tokenize both via tokenizeLocal, join each with spaces, delegate to `TokenSimilarity.overlap()`. The space-join round-trip works because TokenSimilarity splits on whitespace internally.

    6. `isFreeEmailDomain(domain: String): Boolean` — `domain.lowercase() in FREE_EMAIL_DOMAINS`.

    Run tests — all must pass.

    REFACTOR: Add KDoc to the object and each public method. Ensure consistent formatting with NicknameExpander/TokenSimilarity.
  </action>
  <verify>
    <automated>cd /home/jared/dev/worktrees/identity-resolution-enrichment/core && ./gradlew test --tests "riven.core.service.identity.EmailMatcherTest"</automated>
  </verify>
  <done>All EmailMatcher unit tests pass. extractDomain/extractLocal handle null cases. tokenizeLocal splits on all three delimiters and lowercases. localPartSimilarity delegates to TokenSimilarity.overlap. isFreeEmailDomain checks against 31+ domain set case-insensitively.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "riven.core.service.identity.EmailMatcherTest"` passes
- EmailMatcher.kt exists as Kotlin object in service.identity package
- All 5 public methods implemented per CONTEXT.md spec
- FREE_EMAIL_DOMAINS contains ~31 entries
</verification>

<success_criteria>
- EmailMatcher.extractDomain and extractLocal correctly parse email addresses
- EmailMatcher.tokenizeLocal splits on `.`, `_`, `-` and filters empties
- EmailMatcher.localPartSimilarity returns correct overlap coefficients
- EmailMatcher.isFreeEmailDomain identifies all major free providers
- Full unit test coverage for all public methods including edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/04-email-decomposition-strategy/04-01-SUMMARY.md`
</output>
