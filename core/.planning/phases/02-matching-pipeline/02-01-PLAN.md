---
phase: 02-matching-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/riven/core/enums/identity/MatchSignalType.kt
  - src/main/kotlin/riven/core/models/identity/CandidateMatch.kt
  - src/main/kotlin/riven/core/models/identity/ScoredCandidate.kt
  - src/main/kotlin/riven/core/models/identity/MatchSignal.kt
  - src/main/kotlin/riven/core/enums/activity/Activity.kt
  - src/main/kotlin/riven/core/enums/core/ApplicationEntityType.kt
  - src/main/kotlin/riven/core/entity/identity/MatchSuggestionEntity.kt
  - src/main/kotlin/riven/core/models/identity/MatchSuggestion.kt
  - src/main/kotlin/riven/core/repository/identity/MatchSuggestionRepository.kt
  - src/test/kotlin/riven/core/service/util/factory/identity/IdentityFactory.kt
autonomous: true
requirements: [MATCH-04, MATCH-06, SUGG-01, SUGG-02, SUGG-03, SUGG-04]

must_haves:
  truths:
    - "MatchSignalType enum exists with default weights for EMAIL(0.9), PHONE(0.85), NAME(0.5), COMPANY(0.3), CUSTOM_IDENTIFIER(0.7)"
    - "CandidateMatch, ScoredCandidate, and MatchSignal domain models exist as data classes"
    - "MatchSuggestionEntity.signals is List<Map<String, Any?>> not Map<String, Any?>"
    - "MatchSuggestionRepository has custom queries for active and rejected suggestion lookup"
    - "Activity.MATCH_SUGGESTION and ApplicationEntityType.MATCH_SUGGESTION enum values exist"
    - "IdentityFactory test factory produces MatchSuggestionEntity, CandidateMatch, ScoredCandidate, MatchSignal"
  artifacts:
    - path: "src/main/kotlin/riven/core/enums/identity/MatchSignalType.kt"
      provides: "Signal type enum with default weights"
      contains: "DEFAULT_WEIGHTS"
    - path: "src/main/kotlin/riven/core/models/identity/CandidateMatch.kt"
      provides: "Raw candidate from DB query"
    - path: "src/main/kotlin/riven/core/models/identity/ScoredCandidate.kt"
      provides: "Scored result for persistence"
    - path: "src/main/kotlin/riven/core/models/identity/MatchSignal.kt"
      provides: "Individual signal detail"
    - path: "src/test/kotlin/riven/core/service/util/factory/identity/IdentityFactory.kt"
      provides: "Test factory for identity domain objects"
  key_links:
    - from: "MatchSignalType"
      to: "IdentityMatchScoringService (Plan 02)"
      via: "DEFAULT_WEIGHTS companion object"
      pattern: "MatchSignalType\\.DEFAULT_WEIGHTS"
    - from: "MatchSuggestionRepository"
      to: "IdentityMatchSuggestionService (Plan 03)"
      via: "findActiveSuggestion/findRejectedSuggestion"
      pattern: "findActiveSuggestion|findRejectedSuggestion"
---

<objective>
Define all domain models, enums, repository queries, and test factories that the matching pipeline services depend on. Fix the signals JSONB field type from Map to List.

Purpose: Establish contracts before any service implementation — Plan 02, 03, and 04 all consume these types without needing codebase exploration.
Output: MatchSignalType enum, 3 domain model files, updated MatchSuggestionEntity/Repository, enum additions, test factory.
</objective>

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-matching-pipeline/02-CONTEXT.md
@.planning/phases/02-matching-pipeline/02-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From src/main/kotlin/riven/core/entity/identity/MatchSuggestionEntity.kt:
```kotlin
@Entity @Table(name = "match_suggestions")
data class MatchSuggestionEntity(
    val id: UUID? = null,
    val workspaceId: UUID,
    val sourceEntityId: UUID,
    val targetEntityId: UUID,
    var status: MatchSuggestionStatus = MatchSuggestionStatus.PENDING,
    val confidenceScore: BigDecimal,
    var signals: JsonObject = emptyMap(),           // CHANGE to List<Map<String, Any?>>
    var rejectionSignals: JsonObject? = null,        // KEEP as Map<String, Any?> — flat snapshot
    var resolvedBy: UUID? = null,
    var resolvedAt: ZonedDateTime? = null,
) : AuditableSoftDeletableEntity()
```

From src/main/kotlin/riven/core/models/identity/MatchSuggestion.kt:
```kotlin
data class MatchSuggestion(
    val signals: JsonObject,                        // CHANGE to List<Map<String, Any?>>
    // ... other fields unchanged
)
```

From src/main/kotlin/riven/core/enums/common/validation/SchemaType.kt:
```kotlin
enum class SchemaType { TEXT, OBJECT, NUMBER, CHECKBOX, DATE, DATETIME, RATING, PHONE, EMAIL, URL, CURRENCY, PERCENTAGE, SELECT, MULTI_SELECT, FILE_ATTACHMENT, LOCATION, ID }
```

From src/main/kotlin/riven/core/entity/entity/EntityAttributeEntity.kt:
```kotlin
data class EntityAttributeEntity(
    val id: UUID?, val entityId: UUID, val workspaceId: UUID,
    val typeId: UUID, val attributeId: UUID,
    val schemaType: SchemaType, val value: JsonNode,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MatchSignalType enum and domain model data classes</name>
  <files>
    src/main/kotlin/riven/core/enums/identity/MatchSignalType.kt,
    src/main/kotlin/riven/core/models/identity/CandidateMatch.kt,
    src/main/kotlin/riven/core/models/identity/ScoredCandidate.kt,
    src/main/kotlin/riven/core/models/identity/MatchSignal.kt
  </files>
  <action>
    Create four new files:

    **MatchSignalType.kt** (`riven.core.enums.identity`):
    - Enum values: EMAIL, PHONE, NAME, COMPANY, CUSTOM_IDENTIFIER (all with `@JsonProperty`)
    - Companion object with `val DEFAULT_WEIGHTS: Map<MatchSignalType, Double>` containing: EMAIL=0.9, PHONE=0.85, NAME=0.5, COMPANY=0.3, CUSTOM_IDENTIFIER=0.7
    - Add a companion function `fun fromSchemaType(schemaType: SchemaType): MatchSignalType` that maps SchemaType.EMAIL -> EMAIL, SchemaType.PHONE -> PHONE, and all other IDENTIFIER-classified types -> CUSTOM_IDENTIFIER. NAME and COMPANY are derived contextually by the scoring service, not from SchemaType (they fall through to CUSTOM_IDENTIFIER here; the scoring service will use attribute name heuristics if needed, or the caller specifies).

    **CandidateMatch.kt** (`riven.core.models.identity`):
    - Data class: `candidateEntityId: UUID, candidateAttributeId: UUID, candidateValue: String, signalType: MatchSignalType, similarityScore: Double`
    - This is the raw output from the pg_trgm candidate query, one row per matching attribute.

    **MatchSignal.kt** (`riven.core.models.identity`):
    - Data class: `type: MatchSignalType, sourceValue: String, targetValue: String, similarity: Double, weight: Double`
    - This is the per-signal detail stored in the JSONB breakdown.
    - Add `fun toMap(): Map<String, Any?>` that returns `mapOf("type" to type.name, "sourceValue" to sourceValue, "targetValue" to targetValue, "similarity" to similarity, "weight" to weight)`.

    **ScoredCandidate.kt** (`riven.core.models.identity`):
    - Data class: `sourceEntityId: UUID, targetEntityId: UUID, compositeScore: Double, signals: List<MatchSignal>`
    - This is the output of scoring, input to suggestion persistence.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>Four files compile successfully. MatchSignalType has DEFAULT_WEIGHTS with correct values. All model data classes have the fields specified.</done>
</task>

<task type="auto">
  <name>Task 2: Fix signals type, add enum values, custom repo queries, and test factory</name>
  <files>
    src/main/kotlin/riven/core/entity/identity/MatchSuggestionEntity.kt,
    src/main/kotlin/riven/core/models/identity/MatchSuggestion.kt,
    src/main/kotlin/riven/core/repository/identity/MatchSuggestionRepository.kt,
    src/main/kotlin/riven/core/enums/activity/Activity.kt,
    src/main/kotlin/riven/core/enums/core/ApplicationEntityType.kt,
    src/test/kotlin/riven/core/service/util/factory/identity/IdentityFactory.kt
  </files>
  <action>
    **MatchSuggestionEntity.kt changes:**
    - Change `signals` field type from `JsonObject` (Map) to `List<Map<String, Any?>>` with default `emptyList()`. Keep `@Type(JsonBinaryType::class)` and `columnDefinition = "jsonb"` — Hypersistence handles List types transparently.
    - Keep `rejectionSignals` as `JsonObject?` (Map) — it stores a flat snapshot of the signals at rejection time, not an array.
    - Update `toModel()` to pass the new `signals` type.

    **MatchSuggestion.kt changes:**
    - Change `signals` field type from `JsonObject` to `List<Map<String, Any?>>`.

    **MatchSuggestionRepository.kt additions:**
    Add two native queries (per RESEARCH.md Pattern 5):

    1. `findActiveSuggestion(workspaceId: UUID, sourceEntityId: UUID, targetEntityId: UUID): MatchSuggestionEntity?` — native query: `SELECT * FROM match_suggestions WHERE workspace_id = :workspaceId AND source_entity_id = :sourceEntityId AND target_entity_id = :targetEntityId AND deleted = false LIMIT 1`

    2. `findRejectedSuggestion(workspaceId: UUID, sourceEntityId: UUID, targetEntityId: UUID): MatchSuggestionEntity?` — native query: `SELECT * FROM match_suggestions WHERE workspace_id = :workspaceId AND source_entity_id = :sourceEntityId AND target_entity_id = :targetEntityId AND status = 'REJECTED' AND deleted = true ORDER BY created_at DESC LIMIT 1`

    **Activity.kt:** Add `@JsonProperty("MATCH_SUGGESTION") MATCH_SUGGESTION,` to the enum.

    **ApplicationEntityType.kt:** Add `@JsonProperty("MATCH_SUGGESTION") MATCH_SUGGESTION,` to the enum.

    **IdentityFactory.kt** (`riven.core.service.util.factory.identity`):
    Create test factory object with methods:
    - `fun createMatchSuggestionEntity(workspaceId: UUID = UUID.randomUUID(), sourceEntityId: UUID? = null, targetEntityId: UUID? = null, status: MatchSuggestionStatus = MatchSuggestionStatus.PENDING, confidenceScore: BigDecimal = BigDecimal("0.8500"), signals: List<Map<String, Any?>> = emptyList()): MatchSuggestionEntity` — applies canonical UUID ordering on source/target (if both null, generates two random UUIDs and orders them).
    - `fun createMatchSignal(type: MatchSignalType = MatchSignalType.EMAIL, sourceValue: String = "test@example.com", targetValue: String = "test@example.com", similarity: Double = 0.95, weight: Double = 0.9): MatchSignal`
    - `fun createCandidateMatch(candidateEntityId: UUID = UUID.randomUUID(), candidateAttributeId: UUID = UUID.randomUUID(), candidateValue: String = "test@example.com", signalType: MatchSignalType = MatchSignalType.EMAIL, similarityScore: Double = 0.85): CandidateMatch`
    - `fun createScoredCandidate(sourceEntityId: UUID = UUID.randomUUID(), targetEntityId: UUID = UUID.randomUUID(), compositeScore: Double = 0.85, signals: List<MatchSignal> = listOf(createMatchSignal())): ScoredCandidate`
  </action>
  <verify>
    <automated>./gradlew compileKotlin compileTestKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>signals field is List type, both enum values added, two custom repo queries present, IdentityFactory compiles with all four factory methods. `./gradlew build` passes (excluding pre-existing integration test failures).</done>
</task>

</tasks>

<verification>
- `./gradlew build` passes (excluding pre-existing integration test failures in IdentityInfrastructureIntegrationTest)
- MatchSignalType.DEFAULT_WEIGHTS contains all 5 signal types with correct weights
- MatchSuggestionEntity.signals is `List<Map<String, Any?>>` not `Map<String, Any?>`
- MatchSuggestionRepository has findActiveSuggestion and findRejectedSuggestion methods
- Activity.MATCH_SUGGESTION and ApplicationEntityType.MATCH_SUGGESTION exist
- IdentityFactory has 4 factory methods
</verification>

<success_criteria>
All domain models, enums, repository queries, and test factory compile. The signals JSONB field type is corrected. Plans 02, 03, and 04 can import these types directly without any codebase exploration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-matching-pipeline/02-01-SUMMARY.md`
</output>
