---
phase: 03-postgres-adapter-schema-mapping
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - core/src/test/kotlin/riven/core/service/util/factory/CustomSourceTableMappingEntityFactory.kt
  - core/src/test/kotlin/riven/core/service/util/factory/CustomSourceFieldMappingEntityFactory.kt
  - core/src/test/kotlin/riven/core/service/util/factory/PostgresIntrospectionFactory.kt
  - core/src/test/kotlin/riven/core/service/connector/postgres/PgTypeMapperTest.kt
  - core/src/test/kotlin/riven/core/service/connector/postgres/SchemaHashTest.kt
  - core/src/test/kotlin/riven/core/service/connector/postgres/PostgresAdapterTest.kt
  - core/src/test/kotlin/riven/core/service/connector/pool/WorkspaceConnectionPoolManagerTest.kt
  - core/src/test/kotlin/riven/core/service/connector/mapping/CustomSourceSchemaInferenceServiceTest.kt
  - core/src/test/kotlin/riven/core/service/connector/mapping/CustomSourceFieldMappingServiceTest.kt
  - core/src/test/kotlin/riven/core/service/connector/mapping/NlMappingSuggestionServiceTest.kt
  - core/src/test/kotlin/riven/core/controller/connector/CustomSourceMappingControllerTest.kt
autonomous: true
requirements:
  - PG-01
  - PG-02
  - PG-03
  - PG-04
  - PG-05
  - PG-06
  - PG-07
  - MAP-01
  - MAP-02
  - MAP-06
  - MAP-08

must_haves:
  truths:
    - "Test suite remains green (./gradlew test passes) after this plan lands"
    - "Every downstream Phase 3 plan has a placeholder test class to populate (no scavenger hunt)"
    - "Factories exist for CustomSourceTableMappingEntity and CustomSourceFieldMappingEntity per project rule (never inline-construct entities)"
    - "Test classes compile against currently non-existent SUT classes via @Disabled placeholder pattern"
  artifacts:
    - path: "core/src/test/kotlin/riven/core/service/util/factory/CustomSourceTableMappingEntityFactory.kt"
      provides: "Factory.create(...) returning a populated CustomSourceTableMappingEntity for tests"
    - path: "core/src/test/kotlin/riven/core/service/util/factory/CustomSourceFieldMappingEntityFactory.kt"
      provides: "Factory.create(...) returning a populated CustomSourceFieldMappingEntity for tests"
    - path: "core/src/test/kotlin/riven/core/service/util/factory/PostgresIntrospectionFactory.kt"
      provides: "Factory builders for SchemaIntrospectionResult / TableSchema / ColumnSchema test fixtures"
    - path: "core/src/test/kotlin/riven/core/service/connector/postgres/PgTypeMapperTest.kt"
      provides: "@Disabled placeholder + named test methods covering every pg-type mapping branch (text, int, bool, date, timestamp, uuid, enum, jsonb, array, bytea, geometry, unknown)"
    - path: "core/src/test/kotlin/riven/core/service/connector/postgres/SchemaHashTest.kt"
      provides: "@Disabled placeholder asserting determinism + column-order independence"
    - path: "core/src/test/kotlin/riven/core/service/connector/postgres/PostgresAdapterTest.kt"
      provides: "@Disabled placeholder for fetchRecords cursor path, PK fallback, OBJECT round-trip, empty result"
    - path: "core/src/test/kotlin/riven/core/service/connector/pool/WorkspaceConnectionPoolManagerTest.kt"
      provides: "@Disabled placeholder for pool caching, eviction on PATCH, eviction on delete, @PreDestroy"
    - path: "core/src/test/kotlin/riven/core/service/connector/mapping/CustomSourceSchemaInferenceServiceTest.kt"
      provides: "@Disabled placeholder for /schema response, drift detection, FK surfacing"
    - path: "core/src/test/kotlin/riven/core/service/connector/mapping/CustomSourceFieldMappingServiceTest.kt"
      provides: "@Disabled placeholder for Save → EntityType creation + FK relationships + composite-FK skip"
    - path: "core/src/test/kotlin/riven/core/service/connector/mapping/NlMappingSuggestionServiceTest.kt"
      provides: "@Disabled placeholder for cache hit/miss + LLM failure → empty suggestions"
    - path: "core/src/test/kotlin/riven/core/controller/connector/CustomSourceMappingControllerTest.kt"
      provides: "@Disabled placeholder for /schema GET + Save POST + cursor-index-warning payload"
  key_links:
    - from: "Wave 1 plans (03-01, 03-02)"
      to: "factories under service/util/factory/"
      via: "test classes call Factory.create(...) instead of constructing entities inline"
      pattern: "CustomSource(Table|Field)MappingEntityFactory.create"
    - from: "Wave 1+ plans"
      to: "@Disabled placeholder test classes"
      via: "remove @Disabled and replace placeholder() body with real assertions in their own plan"
      pattern: "@Disabled\\(\"populated by plan"
---

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-postgres-adapter-schema-mapping/03-CONTEXT.md
@core/CLAUDE.md
@.planning/phases/02-secure-connection-management/02-00-PLAN.md

# Established Wave-0 pattern (from Phase 2 Plan 02-00):
# - Each test class compiles against the SUT it will eventually exercise
# - Bodies are placeholder() — a private helper that no-ops to keep JUnit happy
# - @Disabled("populated by plan {phase}-{plan}") on every test method
# - Downstream plans flip @Disabled off and replace placeholder() with real assertions
# - Factories under service/util/factory/ are populated immediately (used by Wave-1 plans)

<interfaces>
<!-- Existing factories already in place; mirror their shape -->

From core/src/test/kotlin/riven/core/service/util/factory/CustomSourceConnectionEntityFactory.kt:
- object CustomSourceConnectionEntityFactory { fun create(...) }

From core/src/main/kotlin/riven/core/entity/connector/DataConnectorConnectionEntity.kt:
- class DataConnectorConnectionEntity(id, workspaceId, name, connectionStatus, encryptedCredentials, iv, keyVersion, lastVerifiedAt, lastFailureReason)

From core/src/main/kotlin/riven/core/models/ingestion/adapter/SchemaIntrospectionResult.kt:
- data class SchemaIntrospectionResult(tables: List<TableSchema>)
- data class TableSchema(name: String, columns: List<ColumnSchema>)
- data class ColumnSchema(name: String, typeLiteral: String, nullable: Boolean)

Future Phase 3 entities (you create the factories now; the entity classes themselves land in 03-01):
- CustomSourceTableMappingEntity(id, workspaceId, connectionId, tableName, lifecycleDomain, semanticGroup, entityTypeId, schemaHash, lastIntrospectedAt, published)
- CustomSourceFieldMappingEntity(id, workspaceId, connectionId, tableName, columnName, pgDataType, nullable, isPrimaryKey, isForeignKey, fkTargetTable, fkTargetColumn, attributeName, schemaType, isIdentifier, isSyncCursor, isMapped, stale)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add factories under service/util/factory/</name>
  <files>
    core/src/test/kotlin/riven/core/service/util/factory/CustomSourceTableMappingEntityFactory.kt,
    core/src/test/kotlin/riven/core/service/util/factory/CustomSourceFieldMappingEntityFactory.kt,
    core/src/test/kotlin/riven/core/service/util/factory/PostgresIntrospectionFactory.kt
  </files>
  <action>
    Create three Kotlin `object` factories. Each exposes a `create(...)` builder with sensible defaults so tests can override only the fields under inspection. Mirror the shape of `CustomSourceConnectionEntityFactory` (Phase 2).

    For `CustomSourceTableMappingEntityFactory.create(...)`:
      - Defaults: random `workspaceId` UUID, random `connectionId` UUID, `tableName="customers"`, `lifecycleDomain=LifecycleDomain.UNCATEGORIZED`, `semanticGroup=SemanticGroup.UNCATEGORIZED`, `entityTypeId=null`, `schemaHash="hash-${UUID.randomUUID()}"`, `lastIntrospectedAt=ZonedDateTime.now()`, `published=false`. Returns `CustomSourceTableMappingEntity` (class created by 03-01 — leave the import; the file fails to compile until 03-01 lands, which is intentional and acceptable for Wave 0 — see Phase 2 02-00 precedent which used the same pattern with `CustomSourceConnectionEntityFactory`).

    For `CustomSourceFieldMappingEntityFactory.create(...)`:
      - Defaults: random `workspaceId` + `connectionId`, `tableName="customers"`, `columnName="email"`, `pgDataType="text"`, `nullable=false`, `isPrimaryKey=false`, `isForeignKey=false`, `fkTargetTable=null`, `fkTargetColumn=null`, `attributeName="email"`, `schemaType=SchemaType.EMAIL`, `isIdentifier=false`, `isSyncCursor=false`, `isMapped=true`, `stale=false`. Returns `CustomSourceFieldMappingEntity`.

    For `PostgresIntrospectionFactory`:
      - Re-uses existing `SchemaIntrospectionResult`, `TableSchema`, `ColumnSchema` (already in place — Phase 1).
      - `column(name="id", type="uuid", nullable=false): ColumnSchema`
      - `table(name="customers", columns=listOf(column())): TableSchema`
      - `result(tables=listOf(table())): SchemaIntrospectionResult`

    All factories are top-level `object`s, NOT Spring beans. Do NOT add KDoc beyond a one-line description per CONVENTIONS.md (factories are utility, not API surface).

    Compilation note: `CustomSourceTableMappingEntity` and `CustomSourceFieldMappingEntity` do NOT yet exist. You must therefore create EMPTY shell entity files at:
      - core/src/main/kotlin/riven/core/entity/connector/CustomSourceTableMappingEntity.kt
      - core/src/main/kotlin/riven/core/entity/connector/CustomSourceFieldMappingEntity.kt
    Each shell is a minimal `class` (empty body) with a primary constructor matching the factory defaults so the project still compiles. Plan 03-01 will replace the shells with full JPA entities, SQL DDL, repositories, and toModel(). Mirror the Phase 2 02-00 pattern of "shell-now, populate-in-01".
  </action>
  <verify>
    <automated>cd core && ./gradlew compileTestKotlin --no-daemon</automated>
  </verify>
  <done>./gradlew compileTestKotlin succeeds; factories exist; downstream plans can call `CustomSourceFieldMappingEntityFactory.create(...)` without recompilation friction.</done>
</task>

<task type="auto">
  <name>Task 2: Create @Disabled placeholder test scaffolds for every Phase 3 SUT</name>
  <files>
    core/src/test/kotlin/riven/core/service/connector/postgres/PgTypeMapperTest.kt,
    core/src/test/kotlin/riven/core/service/connector/postgres/SchemaHashTest.kt,
    core/src/test/kotlin/riven/core/service/connector/postgres/PostgresAdapterTest.kt,
    core/src/test/kotlin/riven/core/service/connector/pool/WorkspaceConnectionPoolManagerTest.kt,
    core/src/test/kotlin/riven/core/service/connector/mapping/CustomSourceSchemaInferenceServiceTest.kt,
    core/src/test/kotlin/riven/core/service/connector/mapping/CustomSourceFieldMappingServiceTest.kt,
    core/src/test/kotlin/riven/core/service/connector/mapping/NlMappingSuggestionServiceTest.kt,
    core/src/test/kotlin/riven/core/controller/connector/CustomSourceMappingControllerTest.kt
  </files>
  <action>
    For each test file create a JUnit-5 class with `@Disabled("populated by plan 03-XX")` (cite the plan that owns it) at the class level + named test methods covering every must-have row from the owning plan. Each test body is a single `placeholder()` call — define `private fun placeholder() {}` at the bottom of the file. This is the exact pattern Phase 2 used in 02-00; do not deviate.

    Test method names per file (one `@Test fun` per name, body = `placeholder()`):

    PgTypeMapperTest (owned by 03-01):
      - `mapsTextFamilyToTEXT`, `mapsNumericFamilyToNUMBER`, `mapsBooleanToCHECKBOX`, `mapsDateToDATE`, `mapsTimestampFamilyToDATETIME`, `mapsUuidPkToID_andUuidNonPkToTEXT`, `mapsEnumToSELECTWithOptions`, `mapsJsonbToOBJECT_preservingStructure`, `mapsArrayToOBJECT`, `mapsByteaToOBJECTBase64`, `mapsGeometryToLOCATION`, `mapsUnknownTypeToOBJECTFallback`

    SchemaHashTest (owned by 03-01):
      - `producesIdenticalHashForSameSchema`, `producesIdenticalHashRegardlessOfColumnOrder`, `producesDifferentHashOnColumnAdd`, `producesDifferentHashOnColumnTypeChange`, `producesDifferentHashOnColumnDrop`

    PostgresAdapterTest (owned by 03-02):
      - `fetchRecordsUsesUpdatedAtCursorWhenColumnPresent`, `fetchRecordsFallsBackToPkInsertsOnlyWhenNoCursor`, `fetchRecordsHonorsLimitStrictly`, `fetchRecordsRoundTripsJsonbAsObjectStructure`, `fetchRecordsReturnsEmptyBatchWithHasMoreFalse`, `introspectSchemaReturnsTablesWithFkMetadata`, `syncModeReturnsPOLL`, `mapsPostgresAuthFailureToFatalAdapterAuthException`, `mapsPostgresTimeoutToTransientAdapterException`

    WorkspaceConnectionPoolManagerTest (owned by 03-02):
      - `getPoolReturnsCachedInstanceOnSubsequentCalls`, `evictClosesPoolAndRemovesEntry`, `evictAllClosesAllPools`, `preDestroyClosesAllPools`, `getPoolUsesProvidedCredentialsForFirstBuild`, `poolConfiguresMaxPoolSize2_idleTimeout10m_maxLifetime30m_statementTimeout`

    CustomSourceSchemaInferenceServiceTest (owned by 03-03):
      - `getSchemaReturnsTablesWithComputedSchemaHash`, `getSchemaSurfacesDriftWhenStoredHashDiffers`, `getSchemaSurfacesAddedColumnsAsUnmappedDriftEntries`, `getSchemaMarksDroppedColumnsStaleInMappingTable`, `getSchemaSurfacesFkMetadataPerColumn`, `getSchemaIncludesCursorIndexWarningWhenChosenCursorColumnUnindexed`, `getSchemaScopedToWorkspaceViaPreAuthorize`

    CustomSourceFieldMappingServiceTest (owned by 03-03):
      - `saveCreatesEntityTypeWithSourceTypeConnectorAndReadonlyTrue`, `saveCreatesAttributeDefinitionsForEachMappedColumn`, `saveCreatesRelationshipDefinitionWhenBothFkEndsArePublished`, `saveStoresFkMetadataOnlyWhenTargetTableUnpublished`, `saveSkipsCompositeFkWithUnsupportedNote`, `saveTransitionsTableMappingPublishedTrue`, `saveSurfacesCursorIndexWarningInResponse`, `reSavePropagatesAddedColumnsToEntityType`, `reSaveMarksDroppedFieldsStaleAndKeepsExistingAttributes`, `saveScopedToWorkspaceViaPreAuthorize`

    NlMappingSuggestionServiceTest (owned by 03-04):
      - `suggestionsCachedByConnectionAndSchemaHash`, `cacheHitDoesNotCallLlm`, `cacheMissCallsLlmAndPersistsResult`, `llmTimeoutReturnsEmptySuggestionsWithoutThrowing`, `llmRateLimitReturnsEmptySuggestionsWithoutThrowing`, `inputContainsOnlyTableAndColumnSchemaNeverSampleRows`

    CustomSourceMappingControllerTest (owned by 03-03):
      - `getSchemaReturns200WithTablesAndDriftIndicator`, `saveMappingReturns201WithCreatedEntityTypeId`, `saveMappingResponseIncludesCursorIndexWarningWhenColumnUnindexed`, `getSchemaReturns403WhenWorkspaceMismatch`, `saveMappingValidatesRequestBody`

    Do NOT import the SUT classes that don't yet exist — keep the file self-contained until the owning plan populates it. The placeholder() pattern means you can use `@Disabled` at the class level only and have empty test bodies; no SUT references needed.

    Class-level annotations on every file:
      - `@Disabled("populated by plan 03-XX")` (substitute the owning plan)
      - No Spring annotations; these are pure structural placeholders.
  </action>
  <verify>
    <automated>cd core && ./gradlew test --no-daemon -i 2>&1 | grep -E "(BUILD|test.*Disabled|Tests run)" | tail -20</automated>
  </verify>
  <done>All placeholder test classes compile and the test runner skips them via @Disabled. Total test count unchanged from Phase 2 baseline. Build remains green.</done>
</task>

</tasks>

<verification>
- `./gradlew compileTestKotlin` succeeds.
- `./gradlew test` is green (suite count unchanged; new tests are @Disabled).
- `git status` shows the new files under `core/src/test/kotlin/.../factory/`, `.../postgres/`, `.../pool/`, `.../mapping/`, plus the two shell entity files.
- Downstream plans can grep `@Disabled\\("populated by plan 03-` to find their owned test class.
</verification>

<success_criteria>
Wave 0 done when the project compiles, tests pass (with new files @Disabled), factories exist and are usable from Wave-1 tests, and every Phase 3 SUT has a test-class skeleton ready to populate. No production behaviour changes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-postgres-adapter-schema-mapping/03-00-SUMMARY.md` documenting:
- Files created (test scaffolds + factories + entity shells)
- Confirmed test count delta = 0 (placeholders @Disabled)
- Plan numbers each placeholder test belongs to (so downstream executors can find their work)
</output>
