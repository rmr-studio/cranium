
**Overview**
**Requirements**
1. A `SourceConnector` interface shape that provide a common ground for bespoke extraction and field mapping for wiki ingest.
2. Given the migration away from a structured entity catalog, into a LLM page wiki, there may be an associated `toPage` method, where each interface has access to a spring managed registry of bespoke services and modules in order to extract all necessary information from data sources and create relevant LLM wiki pages. 
3. There would also need to be considerations into how extracted chunks are extracted and hashed for future syncs to ensure that [[2. Source Page Generation, Region Content Hashing & Duplication Skip]] is able to effectively determine unchanged source material. Especially given the fact that source data will be extracted into numerous chunks of data 
